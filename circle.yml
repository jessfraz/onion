machine:
  services:
    - docker

dependencies:
  post:
    # install golint
    - go get github.com/golang/lint/golint

test:
  pre:
    - go vet $(go list ./... | grep -v vendor)
    - golint ./...
    - gofmt -s -l .
  override:
    - make ci
